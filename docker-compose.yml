name: orderflow
services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: orderflow
      POSTGRES_USER: orderflow
      POSTGRES_PASSWORD: orderflow_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U orderflow -d orderflow"]
      interval: 5s
      timeout: 5s
      retries: 15
  kafka:
    image: confluentinc/cp-kafka:8.1.0
    hostname: kafka
    container_name: orderflow-kafka
    ports:
      - "19092:19092"
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: "broker,controller"
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: "CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT"
      KAFKA_CONTROLLER_QUORUM_VOTERS: "1@kafka:29093"
      KAFKA_LISTENERS: "PLAINTEXT://kafka:29092,CONTROLLER://kafka:29093,PLAINTEXT_HOST://0.0.0.0:19092"
      KAFKA_ADVERTISED_LISTENERS: "PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:19092"
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      CLUSTER_ID: MkU3OEVBNTcwNTJENDM2Qk
    healthcheck:
      test: ["CMD-SHELL", "kafka-topics --bootstrap-server localhost:19092 --list"]
      interval: 10s
      timeout: 10s
      retries: 20
  kafka-init:
    image: confluentinc/cp-kafka:8.1.0
    depends_on:
      kafka:
        condition: service_healthy
    entrypoint:
      - /bin/bash
      - -c
    command:
      - |
        set -e

        echo "Creating order.created..."
        kafka-topics --bootstrap-server kafka:29092 \
          --create \
          --if-not-exists \
          --topic order.created \
          --partitions 3 \
          --replication-factor 1

        echo "Creating inventory.reserved..."
        kafka-topics --bootstrap-server kafka:29092 \
          --create \
          --if-not-exists \
          --topic inventory.reserved \
          --partitions 3 \
          --replication-factor 1

        echo "Creating inventory.rejected..."
        kafka-topics --bootstrap-server kafka:29092 \
          --create \
          --if-not-exists \
          --topic inventory.rejected \
          --partitions 3 \
          --replication-factor 1

        echo "Available topics:"
        kafka-topics --bootstrap-server kafka:29092 --list
    restart: "no"

  kafka-ui:
    image: provectuslabs/kafka-ui:v0.7.2
    ports:
      - "8085:8080"
    environment:
      KAFKA_CLUSTERS_0_NAME: orderflow-local
      KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: kafka:29092
    depends_on:
      kafka:
        condition: service_healthy

  inventory-service:
    build:
      context: .
      dockerfile: src/Inventory.Service/Dockerfile
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      # Two endpoints, not one: over cleartext there is no ALPN, so a mixed
      # Http1AndHttp2 endpoint answers h2c with HTTP_1_1_REQUIRED and gRPC cannot
      # connect. gRPC gets an Http2-only port; /health gets an HTTP/1.1 one.
      # ASPNETCORE_URLS is deliberately unset — it would override these endpoints.
      Kestrel__Endpoints__Grpc__Url: http://+:8080
      Kestrel__Endpoints__Grpc__Protocols: Http2
      Kestrel__Endpoints__Health__Url: http://+:8081
      Kestrel__Endpoints__Health__Protocols: Http1
      ConnectionStrings__InventoryDatabase: "Host=postgres;Port=5432;Database=orderflow;Username=orderflow;Password=orderflow_dev"
      Kafka__BootstrapServers: kafka:29092
      Kafka__GroupId: inventory-service
    ports:
      - "5002:8080"
      - "5012:8081"
    depends_on:
      postgres:
        condition: service_healthy
      kafka-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8081/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 20s
    restart: on-failure
  order-api:
    build:
      context: .
      dockerfile: src/Order.Api/Dockerfile
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__OrdersDatabase: "Host=postgres;Port=5432;Database=orderflow;Username=orderflow;Password=orderflow_dev"
      Kafka__BootstrapServers: kafka:29092
      Kafka__GroupId: order-api
      InventoryGrpc__Address: http://inventory-service:8080
    ports:
      - "5001:8080"
    depends_on:
      postgres:
        condition: service_healthy
      kafka-init:
        condition: service_completed_successfully
      # Wait for inventory to actually serve traffic, not merely to have a container.
      inventory-service:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8080/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 20s
    restart: on-failure
  notification-worker:
    build:
      context: .
      dockerfile: src/Notification.Worker/Dockerfile
    environment:
      DOTNET_ENVIRONMENT: Development
      Kafka__BootstrapServers: kafka:29092
      Kafka__GroupId: notification-worker
    depends_on:
      kafka-init:
        condition: service_completed_successfully
    restart: on-failure
volumes:
  postgres-data:
